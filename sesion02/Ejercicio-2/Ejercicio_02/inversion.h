#ifndef INVERSION_H
#define INVERSION_H

const int MAX_ELEMENTOS = 20;

// el original es const para garantizar que no se modifique
void invertirArreglo(const int original[], int invertido[], int size);

#endif